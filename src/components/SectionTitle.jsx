/* eslint-disable react/prop-types */
const SectionTitle = ({ _text }) => {
  return (
    <h1 className="text-center font-semibold pt-12 text-xl lg:text-2xl uppercase -tracking-widest">
      {_text}
    </h1>
  );
};

export default SectionTitle;
